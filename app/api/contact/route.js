import { NextResponse } from "next/server";
function valid(p){return p&&typeof p.name==="string"&&p.name.trim().length>=2&&/^\S+@\S+\.\S+$/.test(p.email||"")&&typeof p.subject==="string"&&p.subject.trim().length>=3&&typeof p.message==="string"&&p.message.trim().length>=20;}
export async function POST(request){try{const payload=await request.json();if(!valid(payload))return NextResponse.json({message:"Please check the required fields."},{status:400});const endpoint=process.env.FORMSPREE_ENDPOINT;if(endpoint){const response=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json","Accept":"application/json"},body:JSON.stringify(payload)});if(!response.ok)return NextResponse.json({message:"The form service rejected the message."},{status:502});return NextResponse.json({message:"Thank you. Your message has been sent."});}return NextResponse.json({message:"Form validated. Add FORMSPREE_ENDPOINT to deliver messages."});}catch{return NextResponse.json({message:"Unable to process the message."},{status:500});}}
