'use client'; import Link from 'next/link'; import {useEffect,useState} from 'react'; export default function Header(){const[dark,setDark]=useState(true);const[open,setOpen]=useState(false);useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light'},[dark]);const nav=['About','Projects','Research','Experience','Skills','Education','Contact'];return <header className="site-header"><div className="container nav-wrap"><Link className="brand" href="#home">SM<span>.</span></Link><nav className="desktop-nav">{nav.map(n=><a key={n} href={'#'+n.toLowerCase()}>{n}</a>)}</nav><div className="nav-actions"><button className="icon-btn" onClick={()=>setDark(!dark)}>◐</button><a className="btn btn-small" href="/Swarnali_Mollick_CV.pdf" download>Download CV</a><button className="icon-btn menu-btn" onClick={()=>setOpen(!open)}>☰</button></div></div>{open&&<nav className="mobile-nav">{nav.map(n=><a key={n} href={'#'+n.toLowerCase()} onClick={()=>setOpen(false)}>{n}</a>)}</nav>}</header>}
