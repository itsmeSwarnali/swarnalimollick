'use client'; import {useState} from 'react'; import ProjectCarousel from './ProjectCarousel'; export default function ProjectShowcase({categories}){const[a,setA]=useState(0);const c=categories[a];return <div className="project-showcase"><div className="project-tabs">{categories.map((x,i)=><button key={x.name} className={i===a?'active':''} onClick={()=>setA(i)}><b>{x.name}</b><span>{x.shortDescription}</span></button>)}</div><div className="category-block"><div className="category-heading"><div><h3>{c.name}</h3><p>{c.description}</p></div></div><ProjectCarousel projects={c.projects}/></div></div>}
