import {skills} from '@/data/skills'; export default function Skills(){return <section id="skills" className="section"><div className="container"><p className="eyebrow">Core Stack</p><h2>Skills I use in real projects</h2><p className="section-lead">A focused view of the tools and methods most relevant to Data Science, Machine Learning and Applied AI roles.</p><div className="skills-grid">{skills.map(s=><article key={s.title}><h3>{s.title}</h3><div className="skill-tags">{s.items.map(i=><span key={i}>{i}</span>)}</div></article>)}</div></div></section>}
