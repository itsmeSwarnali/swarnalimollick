'use client'; import {useState} from 'react'; export default function ContactForm(){const[state,setState]=useState('');async function submit(e){e.preventDefault();setState('Sending…');const fd=new FormData(e.currentTarget);const data=Object.fromEntries(fd);try{const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok)throw new Error(j.error||'Unable to send');setState('Thanks! Your message was sent successfully.');e.currentTarget.reset()}catch(err){setState(err.message)}}return <form className="contact-form" onSubmit={submit}><input className="honeypot" name="website" tabIndex="-1" autoComplete="off"/><div className="form-row"><label>Name<input name="name" required maxLength="80"/></label><label>Email<input type="email" name="email" required maxLength="120"/></label></div><div className="form-row"><label>Company<input name="company" maxLength="120"/></label><label>Subject<input name="subject" required maxLength="150"/></label></div><label>Message<textarea name="message" required minLength="20" maxLength="4000"/></label><button className="btn" type="submit">Send Message</button>{state&&<p className="form-note" role="status">{state}</p>}</form>}
